name: Java Testson

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up JDK
        uses: actions/setup-java@v2
        with:
          java-version: '21'
          distribution: 'adopt'

      - name: Build and test
        run: |
          javac -d build -cp src src/*.java
#          java -cp build:junit-4.13.2.jar org.junit.runner.JUnitCore src.test.*/*.class